{
  "name": "guardrailstask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "integration": "npx cypress run --spec \"cypress/e2e/IntegrationTests/*\"",
    "cypress": "npx cypress run --spec \"cypress/e2e/cypressExercise/**/*\"",
    "all": "npx cypress run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^11.0.1"
  }
}
